export component MainWindow inherits Window {
    width: 400px;
    height: 400px;
    background: #ffff;

    Text {
       y: parent.width / 2;
       x: parent.x + 200px;
       text: "Hello, world";
       color: black;
    }
}
